import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row'
  },
  checkbox_button: {
    height: 24, width: 24,
    justifyContent: 'center',
    alignItems: 'center'
  },
  label: { paddingLeft: 8 },
});