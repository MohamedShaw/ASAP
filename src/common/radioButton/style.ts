import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row'
  },
  radio_button: { height: 24, width: 24, borderRadius: 19, justifyContent: 'center', alignItems: 'center' },
  label: { paddingLeft: 8 },
  active_view: { height: 12, width: 12, borderRadius: 19 }
});