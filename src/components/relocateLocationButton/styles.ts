import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  myLocationContainer: {
    zIndex: 1000,
    position: 'absolute',
    right: 20,

    bottom: '27%',

    height: 41,
    width: 41,
    borderRadius: 41,
    alignItems: 'center',
    justifyContent: 'center',
  },
});
